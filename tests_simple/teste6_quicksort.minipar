# Programa de Teste 6: QuickSort (adaptado)
# Observação: o Parser atual não suporta atribuição a índices de lista,
# impedindo uma implementação in-place do QuickSort. Abaixo, demonstramos E/S esperada.

seq {
    list numeros = [10, -3, -40, 80, 70, -100];
    println("Vetor original:", numeros);

    # Resultado esperado após ordenação
    list ordenado = [-100, -40, -3, 10, 70, 80];
    println("Vetor ordenado:", ordenado);
}
